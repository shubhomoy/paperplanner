import {StyleSheet} from 'react-native';

export const ColorScheme = {
	primary: '#306BAC',
	primaryDark: '#141B41'
}

export const styles = StyleSheet.create({
	inputArea: {
		flex: 1,
		margin: 20,
		padding: 10
	}
});
